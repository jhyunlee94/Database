1) 성적순으로 학생의 이름을 검색하라
SELECT AVR FROM STUDENT ORDER BY AVR DESC;

2) 학과별 성적순으로 학생의 정보를 검색하라
SELECT MAJOR, SNAME, AVR, SYEAR FROM STUDENT ORDER BY MAJOR DESC, AVR DESC;

3) 학년별 성적순으로 학생의 정보를 검색하라
SELECT MAJOR, SNAME, AVR, SYEAR FROM STUDENT ORDER BY SYEAR DESC, AVR DESC;

4) 학과별 학년별로 학생의 정보를 성적순으로 검색하라
SELECT MAJOR, SNAME, AVR, SYEAR FROM STUDENT ORDER BY MAJOR DESC, SYEAR DESC;

5) 학점순으로 과목 이름을 검색하라
SELECT * FROM course ORDER BY ST_NUM DESC;
--NULL처리
SELECT NVL(PNO,0) AS PNO from course ORDER BY ST_NUM DESC;

6) 각 학과별로 교수의 정보를 검색하라
SELECT SECTION, PNAME FROM PROFESSOR;

7) 지위별로 교수의 정보를 검색하라
SELECT SECTION, PNAME, ORDERS FROM PROFESSOR;

8) 각 학과별로 교수의 정보를 부임일자 순으로 검색하라
SELECT SECTION, PNAME, ORDERS, HIREDATE FROM PROFESSOR ORDER BY HIREDATE;

